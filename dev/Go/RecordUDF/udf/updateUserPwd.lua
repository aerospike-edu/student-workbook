function updatePassword(topRec,pwd)
   -- Log current password
   x 
   -- Assign new password to the user record
   x 
   -- Update user record
   x 
   -- Log new password
   x 
   -- return new password
   return x
end
